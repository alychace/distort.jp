# The URL the site will be built for
base_url = "https://distort.jp"

# The site title and description; used in feeds by default.
title = "Specular Anomalies"
description = "Reports from dead-end steets."

author = "aly"
social = "https://mastodon.social/alyc"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# rss feed
generate_feed = true

taxonomies = [{ name = "tags" }]

default_language = "en"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

highlight_theme = "OneHalfLight"

[extra]
image = "/img/acc_guide.jpg"

# control dark mode: auto | dark | toggle
mode = "auto"

# interal CDN resource
useCDN = false

favicon = "/icon/favicon.png"

copyright = "aly"

ga = "G-040DR443G9"

[extra.translations]
# languages = [{name = "en", url = "/"}]

[[extra.translations.en]]
show_more = "Read more ⟶"
previous_page = "← Previous"
next_page = "Next →"
posted_on = "on "
posted_by = "Published by"
read_time = "minute read"
all_tags = "All tags"
menus = [
    { name = "Home", url = "/", weight = 2 },
    { name = "Archive", url = "/posts", weight = 2 },
#    { name = "About", url = "/about", weight = 3 },
]

# config social icon info in the footer
[[extra.social]]
icon = "at-sign"
name = "Mastodon"
url = "https://mastodon.social/@alyc"

[[extra.social]]
icon = "github"
name = "GitHub"
url = "https://github.com/alychace"
